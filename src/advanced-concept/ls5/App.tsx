/**
 * Fragments
 *
 * A common pattern in React is for a component to return
 * multiple elements. Fragments let you group a list of
 * children without adding extra nodes to the DOM.
 */

import Table from './Table';

export default function App(): JSX.Element {
  return <Table />;
}
